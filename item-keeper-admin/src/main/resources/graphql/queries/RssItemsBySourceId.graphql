query getRssItemsBySourceId($sourceId: ID!) {
    rssItemsBySourceId(sourceId: $sourceId) {
        id
        title
        link
        sourceId
        publicationDate
        description
        category
        guid
        pdaLink
        fullText
        newsId
        type
        tags
        newsLine
    }
}
