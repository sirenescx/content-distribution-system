query getRssItemsByPublicationDate($publicationDate: Date!, $page: Int, $size: Int) {
    rssItemsByPublicationDate(publicationDate: $publicationDate, page: $page, size: $size) {
        id
        title
        link
        publicationDate
        description
        category
        guid
        pdaLink
        fullText
        newsId
        type
        tags
        newsLine
        source {
            name
            link
        }
    }
}
