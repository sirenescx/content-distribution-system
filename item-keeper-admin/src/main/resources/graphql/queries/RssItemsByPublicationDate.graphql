query getRssItemsByPublicationDate($publicationDate: Date!) {
    rssItemsByPublicationDate(publicationDate: $publicationDate) {
        id
        title
        link
        sourceId
        publicationDate
        description
        category
        guid
        pdaLink
        fullText
        newsId
        type
        tags
        newsLine
    }
}
