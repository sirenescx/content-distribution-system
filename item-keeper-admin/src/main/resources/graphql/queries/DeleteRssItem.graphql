mutation deleteRssItem($id: ID!) {
    deleteRssItem(id: $id) {
        id
        title
        link
        sourceId
        publicationDate
        description
        category
        guid
        pdaLink
        fullText
        newsId
        type
        tags
        newsLine
        deletedAt
    }
}