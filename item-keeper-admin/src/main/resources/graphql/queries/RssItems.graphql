query getRssItems($page: Int, $size: Int) {
    rssItems(page: $page, size: $size) {
        id
        title
        link
        publicationDate
        description
        category
        guid
        pdaLink
        fullText
        newsId
        type
        tags
        newsLine
        source {
            name
            link
        }
    }
}