query getRssItemById($id: ID!) {
    rssItemById(id: $id) {
        id
        title
        link
        sourceId
        publicationDate
        description
        category
        guid
        pdaLink
        fullText
        newsId
        type
        tags
        newsLine
    }
}
