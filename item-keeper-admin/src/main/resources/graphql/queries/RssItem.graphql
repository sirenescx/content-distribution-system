query getRssItemById($id: ID!) {
    rssItemById(id: $id) {
        id
        title
        link
        publicationDate
        description
        category
        guid
        pdaLink
        fullText
        newsId
        type
        tags
        newsLine
        source {
            name
            link
        }
    }
}
